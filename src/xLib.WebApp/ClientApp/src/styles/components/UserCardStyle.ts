import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
    container: {
        display: 'flex',
        flexDirection: 'row',
        paddingTop: 30,
    },

    avatar: {
        marginTop: 0,
    },
}));

export { useStyles };
